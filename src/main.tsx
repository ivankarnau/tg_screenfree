import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import './styles/App.css'  // Измененный путь

ReactDOM.createRoot(
  document.getElementById('root')!
).render(<App />)